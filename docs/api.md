# API reference

Everything is JSON over HTTP. There is no versioning and no public API — this is
the interface the PISCOC dashboard itself uses, documented so other clients and
scripts can use it too.

The endpoint tables near the bottom were generated by walking the running
server's router:

```bash
npx tsx scripts/generate-api-docs.mjs          # rewrite the tables
npx tsx scripts/generate-api-docs.mjs --check  # fail if they are stale
```

**They have since been edited by hand and no longer match the generator's
output**, so `--check` fails today and re-running the generator would discard
those edits. The hand-added material is the "Public article uploads" group and
the `gated` access level, neither of which the generator can produce. Nothing in
CI runs either command, so the tables are effectively hand-maintained — treat
them as such, and check the routers if a detail matters.

## Access levels

| Level | How a request proves itself |
|---|---|
| **public** | Nothing required. |
| **auth** | Session cookie from `POST /api/login`. |
| **admin** | The same, for a user with the admin flag. |
| **token** | A contributor upload link. The secret in the URL is the whole credential; no session is involved. |
| **webhook** | Shared secret in the `x-webhook-secret` header, set via `WEBHOOK_SECRET`. |

Signing in is `POST /api/login` with `{username, password}`. Failed attempts are
limited to 10 per 15 minutes per IP. User objects never include the password
hash.

Admin covers account management, writing integration credentials and the
public-link toggle. It does **not** cover Airtable syncs, pushes or status reads
— any signed-in user can run those.

## CSRF

Session cookies are issued `SameSite=None` in production so the CMS can be
embedded cross-origin, which means browsers attach them to cross-site requests.
**Every state-changing request therefore needs a CSRF token.**

The server sets a readable `csrf_token` cookie. Echo it in the `x-csrf-token`
header on every `POST`, `PUT`, `PATCH` and `DELETE`:

```js
fetch('/api/articles/1', {
  method: 'PUT',
  headers: { 'Content-Type': 'application/json', 'x-csrf-token': csrfToken },
  credentials: 'include',
  body: JSON.stringify({ title: 'New title' }),
});
```

In the dashboard, `apiRequest()` and `apiUpload()` in
`client/src/lib/queryClient.ts` do this for you. Without the header you get
`403 Missing CSRF token`; with a mismatched one, `403 Invalid CSRF token`.

Three things are exempt, because their callers have no cookie to send:

- the exact path `POST /api/public/team-member-update` — public, gated instead by
  the team-upload setting and a rate limit;
- everything under `/api/public-upload/` — authorized either by the token in the
  URL or, for the token-free routes, by the `article_upload` setting;
- everything under `/api/webhooks/` — authorized by the shared secret.

None of these consults the session, so there is no ambient authority for a
forged cross-site request to borrow.

The match is **exact for the first and by prefix for the other two**. That
distinction is load-bearing: `/api/public/` is not prefix-exempt, because it also
hosts the admin-only `POST /api/public/team-upload-status` and
`POST /api/public/article-upload-status`, which must stay protected.

## Errors

Every error is JSON:

```json
{ "message": "Article not found" }
```

Validation failures add the offending fields:

```json
{
  "message": "Validation error",
  "errors": [{ "path": "title", "message": "Required" }]
}
```

Some errors carry structured context under `details` instead.

| Status | Meaning |
|---|---|
| 400 | Malformed input, failed validation, unusable upload |
| 401 | Not signed in, or an invalid/expired upload link |
| 403 | Signed in but not permitted; missing or bad CSRF token |
| 404 | No such record, or no such endpoint |
| 409 | Conflicts with current state (duplicate username, session already open) |
| 413 | File exceeds the configured limit |
| 429 | Rate limited |
| 500 | Server fault. Details are logged, never returned. |

An unknown path under `/api` returns `404 {"message":"Unknown API endpoint"}`
rather than the dashboard's HTML.

## Rate limits

In-memory and per process, so they are per-instance rather than global.

| Surface | Limit |
|---|---|
| `POST /api/login` | 10 failed attempts / 15 min |
| Contributor and public uploads | 40 / 15 min |
| Upload-link metadata reads | 120 / 15 min |
| Public reads | 300 / 15 min |

All are keyed by IP, including the contributor upload routes — contributors
behind one NAT share a budget.

## Publishing an article

`PUT /api/articles/:id` is the main write path. When the change publishes or
unpublishes the article, the server also pushes it to Airtable and notifies the
live site to drop its cached copy. Both are best-effort: the row is already
saved, so a failure there is logged rather than returned as an error.

Pass `forceWebhook: true` in the body to trigger those effects for an edit that
would not otherwise qualify.

Ordinary draft-to-draft edits deliberately do neither.

## Contributor uploads

An editor mints a link, a contributor uses it, nobody needs an account.

The link is a random 256-bit secret, stored only as a SHA-256 hash and returned
in plaintext exactly once, at creation. It is scoped to one article and an
explicit set of asset types, and lasts 14 days by default
(`UPLOAD_TOKEN_TTL_DAYS`, or `ttlDays` per link).

```
POST   /api/upload-links                        (auth)   -> 201 { token, url, expiresAt, uploadTypes }
GET    /api/upload-links/:articleId             (auth)   -> metadata only, never the secret
DELETE /api/upload-links/:id                    (auth)   revoke

GET    /api/public-upload/:token                (token)  what this link allows
POST   /api/public-upload/:token/image          (token)  multipart, field "file"
POST   /api/public-upload/:token/instagram-image(token)  multipart, field "file"
POST   /api/public-upload/:token/html-zip       (token)  multipart, field "file"
POST   /api/public-upload/:token/complete       (token)  finish a re-upload session
```

## Public article uploads

The token-free alternative: one link for everyone, no per-article secret. A
contributor picks their article from a list and uploads to it.

**The whole surface is behind an admin switch**
(`article_upload.public_link_active`, off by default). While it is off every
route below except the status read answers 403. While it is on, anyone with the
link can replace the images or content of any article on the list, so it is
meant to be opened for a submission window and closed again.

```
GET  /api/public/article-upload-status   (public)  { enabled } — readable while off
POST /api/public/article-upload-status   (admin)   { enabled } — the switch

GET  /api/articles/uploadable            (gated)   [{ id, title, status }]
POST /api/public-upload/image            (gated)   multipart, fields "file" + "articleId"
POST /api/public-upload/instagram-image  (gated)   multipart, fields "file" + "articleId"
POST /api/public-upload/html-zip         (gated)   multipart, fields "file" + "articleId"
```

These are one path segment after `/api/public-upload/`, where the contributor
link routes are two (`/:token/image`), so the two sets cannot collide.

`GET /api/articles/uploadable` lists articles whose status is not `published`,
plus any with an open re-upload session — the same predicate the upload routes
enforce, so an `articleId` that is not on the list is refused with 403 rather
than quietly accepted. Only `id`, `title` and `status` are exposed.

`/api/articles/uploadable` is registered before the articles router, otherwise
that router's `/:id` handler matches `uploadable` and rejects it as a malformed
id.

`GET /api/public-upload/:token` answers with what the contributor needs to render
their page:

```json
{
  "article": { "id": 1, "title": "…", "hasContent": true,
               "hasCoverImage": false, "hasInstagramImage": false },
  "uploadTypes": ["image", "instagram-image", "html-zip"],
  "expiresAt": "2026-09-06T00:00:00.000Z",
  "isReuploadSession": true
}
```

A rejected link is specific about why: not valid, revoked, expired, already used
(all 401), or does not accept that file type (403). Validation runs before the
file is read, so a bad link never causes a byte to be written to disk.

**The multipart field name is `file`** on these routes and on
`/api/direct-upload/*` and `/api/articles/:id/assets/:assetType`. The two ImgBB
upload routes use `image` instead, and `/api/direct-upload/*` additionally
requires an `articleId` form field.

### Uploaded files

Images: JPEG, PNG, GIF, WebP, HEIC/HEIF, AVIF, 10 MB by default. HEIC and AVIF
are transcoded to JPEG. **SVG is rejected** — it is scriptable and these files
are served back to readers.

Archives: ZIP containing HTML plus its images. 50 MB by default, expanding to at
most 200 MB across at most 500 entries and 60 images. Bundled images are
re-hosted and the HTML rewritten to point at them.

**All HTML is sanitized before storage.** Scripts, iframes, event handlers and
`javascript:` URLs are removed. The response reports `sanitized: true` when
anything was stripped — on the contributor and article-asset routes;
`/api/direct-upload/html-zip` returns only `{success, message}`.

Every upload's real format is verified from its leading bytes, not its declared
content type or extension.

## Re-upload sessions

Replacing the content of a live article without serving a half-updated version.

```
POST /api/articles/:id/reupload           (auth)  open a session
POST /api/articles/:id/reupload/complete  (auth)  republish
POST /api/articles/:id/reupload/cancel    (auth)  restore the previous status
POST /api/public-upload/:token/complete   (token) contributor finishes their own session
```

Opening returns `201 { article, uploadUrl, expiresAt }` — note `uploadUrl`, and
no token field; the secret is inside the URL. It drops the article to draft, sets
`isReuploading`, tells the live site to remove it, revokes any older links for
that article and issues one new link covering every asset type. Both publishers
skip the article while the session is open.

Preconditions worth knowing:

| Call | Fails with |
|---|---|
| open, when one is already open | 409 |
| open, on an article that is neither published nor finished | 400 |
| complete or cancel, with no session open | 409 |
| complete, when the article has no content | 400 |

Nothing is published until completion, which republishes, keeps the article's
original publication date, syncs Airtable and refreshes the site cache.

## Integration settings

`/api/integration-settings/*` is admin-only. In practice it holds the Airtable
credential rows — that is the only service anything reads back out of this table.

It **rejects the `imgbb` service** with a 400. That key comes from the
`IMGBB_API_KEY` environment variable and nothing reads the table for it, so
accepting a write would store a credential that silently does nothing.

Values whose key looks like a credential — matching `key`, `token`, `secret`,
`password` or `credential` — come back masked as `••••1234` with
`configured: true` and `redacted: true`. **Other keys are returned verbatim**,
with only a `configured` flag. Table names and toggles are not secrets and are
not masked.

The service-specific read `GET /api/airtable/settings` is `auth` rather than
`admin`; its values are redacted by the same rule. Writing credentials is admin
on every route.

## Webhooks

`POST /api/webhooks/article-published` triggers a full Airtable article sync.

It requires `x-webhook-secret` when `WEBHOOK_SECRET` is set, compared in constant
time. **If `WEBHOOK_SECRET` is unset the endpoint is open** — it only logs a
warning in production. Set it, or anyone can force repeated syncs and exhaust
your Airtable quota.

## Diagnostics

Five Airtable diagnostic routes write to **real Airtable records** to prove the
integration can write. They are admin-only, and they are mounted when
`ENABLE_DIAGNOSTIC_ROUTES=true` **or** whenever `NODE_ENV` is not `production` —
so they are on by default in development.

> Pointing a local server at your production Airtable base will mutate it.

## Carousel quotes and Airtable

The public site reads quotes from Airtable, not from this database, so a quote
only really changes once Airtable does. Every write path therefore reaches
Airtable on its own; nothing waits for someone to press Push.

- `POST /api/carousel-quotes/` and `PUT /api/carousel-quotes/:id` push the quote
  as part of the save, creating its Airtable record if it has none, and answer
  with the stored quote plus `syncedToAirtable`. The push is best-effort: the row
  is stored locally either way, and `syncedToAirtable: false` means the live site
  still shows the old version.
- `DELETE /api/carousel-quotes/:id` removes the Airtable record before the local
  row and answers `{ id, clearedInAirtable }`. `clearedInAirtable: false` means
  the quote is gone from the CMS but Airtable could not be cleared, so the live
  site may still show it until the next push.
- `POST /api/airtable/push/carousel-quotes` **mirrors** rather than merges. It
  overwrites every matching Airtable record, creates the quotes Airtable has not
  seen, and deletes Airtable records no local quote claims. The response carries
  `created`, `updated`, `deleted`, `errors` and `details`.

  The CMS is the source of truth here, so a quote added in Airtable directly is
  deleted by the next push unless it has been pulled in first. Pull, then push.

  One exception: when there are no quotes in the CMS at all, the delete sweep is
  skipped and noted in `details`. An empty local table is far more often a fresh
  or misconfigured database than a deliberate request to empty the base, and
  Airtable has no undo for a batch delete.
- `POST /api/airtable/update-quote/:id` overwrites one record and requires both
  `main` and `philo`; omitting one would blank that column rather than leave it.

`POST /api/airtable/push/team-members` is *not* a mirror — it creates and
updates only. Deleting a team member in the CMS does not remove them from
Airtable.

## All endpoints

<!-- BEGIN GENERATED ROUTES -->
<!-- Originally generated by scripts/generate-api-docs.mjs, since hand-edited. -->
<!-- Re-running the generator will discard the "Public article uploads" group  -->
<!-- and every `gated` row below. See the note at the top of this file.        -->

90 endpoints: 60 auth, 17 admin, 7 public, 5 token, 1 webhook — the tally the
generator produced, which predates the hand-edits and does not count `gated` as
its own bucket.

#### Authentication and users

| | Endpoint | Access |
|---|---|---|
| POST | `/api/login` | public |
| POST | `/api/logout` | public |
| POST | `/api/register` | admin |
| GET | `/api/user` | auth |
| GET | `/api/users` | admin |
| PUT | `/api/users/:id` | admin |
| DELETE | `/api/users/:id` | admin |

#### System

| | Endpoint | Access |
|---|---|---|
| GET | `/api/activity-logs` | auth |
| GET | `/api/health` | public |
| GET | `/api/integration-status` | auth |
| GET | `/api/metrics` | auth |
| GET | `/api/migration-progress` | auth |
| GET | `/api/status` | auth |

#### Articles

| | Endpoint | Access |
|---|---|---|
| GET | `/api/articles/` | auth |
| POST | `/api/articles/` | auth |
| GET | `/api/articles/:id` | auth |
| PUT | `/api/articles/:id` | auth |
| DELETE | `/api/articles/:id` | auth |
| POST | `/api/articles/:id/assets/:assetType` | auth |
| POST | `/api/articles/:id/reupload` | auth |
| POST | `/api/articles/:id/reupload/cancel` | auth |
| POST | `/api/articles/:id/reupload/complete` | auth |
| GET | `/api/articles/featured` | auth |
| GET | `/api/articles/status/:status` | auth |

#### Team members

| | Endpoint | Access |
|---|---|---|
| GET | `/api/team-members/` | auth |
| POST | `/api/team-members/` | auth |
| GET | `/api/team-members/:id` | auth |
| PUT | `/api/team-members/:id` | auth |
| DELETE | `/api/team-members/:id` | auth |
| POST | `/api/team-members/upload-image` | auth |

#### Carousel quotes

| | Endpoint | Access |
|---|---|---|
| GET | `/api/carousel-quotes/` | auth |
| POST | `/api/carousel-quotes/` | auth |
| GET | `/api/carousel-quotes/:id` | auth |
| PUT | `/api/carousel-quotes/:id` | auth |
| DELETE | `/api/carousel-quotes/:id` | auth |
| GET | `/api/carousel-quotes/by-carousel/:carousel` | auth |

#### Admin requests

| | Endpoint | Access |
|---|---|---|
| GET | `/api/admin-requests/` | auth |
| POST | `/api/admin-requests/` | auth |
| GET | `/api/admin-requests/:id` | auth |
| PATCH | `/api/admin-requests/:id` | auth |
| DELETE | `/api/admin-requests/:id` | auth |

#### Image assets

| | Endpoint | Access |
|---|---|---|
| GET | `/api/image-assets/` | auth |
| POST | `/api/image-assets/` | auth |
| GET | `/api/image-assets/:id` | auth |
| DELETE | `/api/image-assets/:id` | auth |

#### Contributor upload links

| | Endpoint | Access |
|---|---|---|
| GET | `/api/public-upload/:token` | token |
| POST | `/api/public-upload/:token/complete` | token |
| POST | `/api/public-upload/:token/html-zip` | token |
| POST | `/api/public-upload/:token/image` | token |
| POST | `/api/public-upload/:token/instagram-image` | token |
| POST | `/api/upload-links` | auth |
| GET | `/api/upload-links/:articleId` | auth |
| DELETE | `/api/upload-links/:id` | auth |

#### Public article uploads

`gated` = refused with 403 unless `article_upload.public_link_active` is on.

| | Endpoint | Access |
|---|---|---|
| GET | `/api/public/article-upload-status` | public |
| POST | `/api/public/article-upload-status` | admin |
| GET | `/api/articles/uploadable` | gated |
| POST | `/api/public-upload/html-zip` | gated |
| POST | `/api/public-upload/image` | gated |
| POST | `/api/public-upload/instagram-image` | gated |

#### Editor uploads

| | Endpoint | Access |
|---|---|---|
| POST | `/api/direct-upload/html-zip` | auth |
| POST | `/api/direct-upload/image` | auth |
| POST | `/api/direct-upload/instagram-image` | auth |

#### Public team profiles

| | Endpoint | Access |
|---|---|---|
| POST | `/api/public/team-member-update` | public |
| GET | `/api/public/team-members-list` | public |
| GET | `/api/public/team-roles` | public |
| GET | `/api/public/team-upload-status` | public |
| POST | `/api/public/team-upload-status` | admin |

#### Airtable

| | Endpoint | Access |
|---|---|---|
| GET | `/api/airtable/debug-schema/:tableId` | auth |
| GET | `/api/airtable/direct-test` | admin |
| POST | `/api/airtable/migrate-to-link-fields/:articleId` | admin |
| POST | `/api/airtable/push/article/:id` | auth |
| POST | `/api/airtable/push/carousel-quotes` | auth |
| POST | `/api/airtable/push/team-members` | auth |
| GET | `/api/airtable/settings` | auth |
| POST | `/api/airtable/settings` | admin |
| POST | `/api/airtable/sync/articles` | auth |
| POST | `/api/airtable/sync/carousel-quotes` | auth |
| POST | `/api/airtable/sync/team-members` | auth |
| POST | `/api/airtable/test-batch-migration` | admin |
| GET | `/api/airtable/test-connection` | auth |
| POST | `/api/airtable/test-link/:articleId` | admin |
| POST | `/api/airtable/test-migration/:articleId` | admin |
| POST | `/api/airtable/update-api-key` | admin |
| POST | `/api/airtable/update-quote/:id` | auth |
| POST | `/api/airtable/update/article/:id` | auth |
| POST | `/api/airtable/upload-image-url/:articleId/:fieldName` | auth |
| POST | `/api/airtable/upload-image/:articleId/:fieldName` | auth |

#### ImgBB

| | Endpoint | Access |
|---|---|---|
| GET | `/api/imgbb/status` | auth |
| POST | `/api/imgbb/upload-to-airtable/:articleId/:fieldName` | auth |
| POST | `/api/imgbb/upload-url-to-airtable/:articleId/:fieldName` | auth |

#### Integration settings

| | Endpoint | Access |
|---|---|---|
| POST | `/api/integration-settings/` | admin |
| PUT | `/api/integration-settings/:id` | admin |
| DELETE | `/api/integration-settings/:id` | admin |
| GET | `/api/integration-settings/:service` | admin |
| GET | `/api/integration-settings/:service/:key` | admin |

#### Webhooks

| | Endpoint | Access |
|---|---|---|
| POST | `/api/webhooks/article-published` | webhook |
<!-- END GENERATED ROUTES -->

Counts reflect a development boot. In production the five diagnostic routes are
absent unless `ENABLE_DIAGNOSTIC_ROUTES=true`.
