node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment variables
.env
.env.local
.env.development
.env.test
.env.production

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea
.vscode
*.swp
*.swo

# Database files
*.sqlite
*.sqlite3

# Temporary files
.tmp
.temp
temp/
tests/temp/

# Runtime upload staging. Uploads are written to the OS temp directory and
# removed when the response finishes; this covers anything left by older builds.
uploads/

# Local TLS certificates for HTTPS development. These are generated by
# scripts/setup-https.js — a private key must never be committed.
certs/
*.pem
*.key

# Test output — note that test *sources* are intentionally tracked.
coverage/

# Replit files (removed for local development)
.replit
replit.nix
.replit.toml

# Docker volumes
postgres_data/piscoc_backup.sql
.vercelignore
.vercel
.playwright-mcp/
